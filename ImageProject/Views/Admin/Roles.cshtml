@{
    ViewData["Title"] = "AdminPanel";
}

<partial name="_AdminHeader" />
<div id="partialPlace">
    <table class="table">
        <tr>
            <th>Name</th>
            <th></th>
        </tr>
        @foreach (var role in Model)
        {
            <tr>
                <td>@role.Name</td>
            </tr>
        }
    </table>
</div>

